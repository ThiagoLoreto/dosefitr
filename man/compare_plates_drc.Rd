% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_plates_drc.R
\name{compare_plates_drc}
\alias{compare_plates_drc}
\title{Compare Dose-Response Curves Across Plates}
\usage{
compare_plates_drc(
  batch_drc_result,
  compare_by = "compound",
  output_dir = "plate_comparison_plots",
  plot_width = 10,
  plot_height = 8,
  plot_dpi = 600,
  y_limits = NULL,
  y_axis_title = NULL,
  color_palette = "set1",
  show_error_bars = TRUE,
  show_grid = FALSE,
  legend_position = "right",
  legend_title = "Plate",
  legend_text_size = 11,
  legend_title_size = 11,
  legend_ncol = NULL,
  legend_label_wrap = 25,
  show_legend = TRUE,
  axis_title_size = 14,
  axis_title_color = "black",
  axis_text_size = 12,
  axis_text_color = "black",
  colors = NULL,
  point_size = NULL,
  point_shapes = NULL,
  error_bar_width = 0.05,
  selected_entities = NULL,
  min_plates = 2,
  x_limits = NULL,
  x_limits_scale = "log10",
  x_axis_title = NULL,
  curve_linewidth = 1,
  curve_alpha = 0.7,
  show_ic50_lines = FALSE,
  plot_title_size = NULL,
  axis_line_color = "black",
  show_border = FALSE,
  transparent_background = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{batch_drc_result}{The list returned by \code{\link[=batch_drc_analysis]{batch_drc_analysis()}}.
Must contain a \code{drc_results} element (one entry per plate), each with
a \code{drc_result} sub-list that holds \code{detailed_results}.}

\item{compare_by}{Character.  Controls the \strong{plot title} shown inside
each image.  Plots are always separated by the full \code{Construct:Compound}
pair regardless of this setting, so the same compound on different
constructs always produces separate files.
\describe{
\item{\code{"compound"}}{(default) Title shows the compound name.}
\item{\code{"construct"}}{Title shows the construct / target name.}
}}

\item{output_dir}{Character.  Directory where the PNG files are saved.
Created automatically if it does not exist.  Defaults to
\code{"plate_comparison_plots"} inside the current working directory.}

\item{plot_width, plot_height}{Numeric.  Plot dimensions in inches.
Defaults: \code{10} x \code{8}.}

\item{plot_dpi}{Numeric.  Resolution for saved PNGs.  Default: \code{600}.}

\item{y_limits}{Numeric vector of length 2 for fixed y-axis limits, or
\code{NULL} (default) to let each plot auto-scale.}

\item{y_axis_title}{Character.  Y-axis label.  If \code{NULL} (default),
auto-detected from the batch result: \code{"Cell Viability (\%)"} or
\code{"Luminescence"} for viability assays, \code{"Normalised BRET ratio [\%]"}
or \code{"BRET ratio"} for NanoBRET assays (depending on whether
\code{normalize} was \code{TRUE} or \code{FALSE}).}

\item{color_palette}{Character.  Any palette name accepted by
\code{\link[=plot_multiple_compounds]{plot_multiple_compounds()}} (e.g. \code{"set1"}, \code{"okabe_ito"},
\code{"nature"}).  Default: \code{"set1"}.}

\item{show_error_bars}{Logical.  Show mean +- SD error bars on data
points.  Default: \code{TRUE}.}

\item{show_grid}{Logical.  Show background grid lines.  Default: \code{FALSE}.}

\item{legend_position}{Character.  One of \code{"right"}, \code{"left"}, \code{"top"},
\code{"bottom"}, \code{"none"}.  Default: \code{"right"}.}

\item{legend_title}{Character.  Title printed above the legend.
Default: \code{"Plate"}.}

\item{legend_text_size}{Numeric.  Font size for the legend item labels
(plate names).  Default: \code{11}.}

\item{legend_title_size}{Numeric.  Font size for the legend title.
Default: \code{11}.}

\item{legend_ncol}{Integer.  Number of columns in the legend.  \code{NULL}
(default) uses adaptive logic from \code{\link[=plot_multiple_compounds]{plot_multiple_compounds()}}.}

\item{legend_label_wrap}{Integer.  Maximum character width before legend
labels wrap to a new line.  Default: \code{25}.}

\item{show_legend}{Logical.  Whether to display the legend.  Default: \code{TRUE}.}

\item{axis_title_size}{Numeric.  Font size for axis titles.  Default: \code{14}.}

\item{axis_title_color}{Character.  Colour of axis title text.  Default:
\code{"black"}.}

\item{axis_text_size}{Numeric.  Font size for axis tick labels.
Default: \code{12}.}

\item{axis_text_color}{Character.  Colour of axis tick labels.  Default:
\code{"black"}.}

\item{colors}{Either \code{NULL} (use \code{color_palette}), a character vector of
hex/colour names (one per plate), or \code{TRUE} for automatic ggplot2 hues.}

\item{point_size}{Numeric.  Override the automatic point size.  \code{NULL}
(default) uses the same adaptive logic as \code{\link[=plot_multiple_compounds]{plot_multiple_compounds()}}.}

\item{point_shapes}{Numeric vector of point shape codes, one per plate.
\code{NULL} uses automatic shapes.}

\item{error_bar_width}{Numeric.  Width of the error bar caps.  Default:
\code{0.05}.}

\item{selected_entities}{Character vector.  Restrict the comparison to a
subset of compound or construct names.  \code{NULL} (default) processes all
entities found across plates.}

\item{min_plates}{Integer.  Minimum number of plates that must contain an
entity for a plot to be generated.  Default: \code{2} (skip entities that
appear on only one plate, as there is nothing to compare).}

\item{x_limits}{Numeric vector of length 2 specifying the x-axis limits.
\code{NULL} (default) auto-calculates from the data.  Unit is controlled by
\code{x_limits_scale}.  Passed directly to \code{\link[=plot_multiple_compounds]{plot_multiple_compounds()}}.}

\item{x_limits_scale}{Character.  Unit of \code{x_limits}: \code{"log10"} (default),
\code{"molar"}, \code{"uM"}, or \code{"nM"}.  Ignored when \code{x_limits = NULL}.}

\item{x_axis_title}{Character.  Custom x-axis label.  \code{NULL} (default) uses
the standard \eqn{Log_{10}} Concentration [M] label.}

\item{curve_linewidth}{Numeric.  Line width of the fitted curves.
Default: \code{1}.}

\item{curve_alpha}{Numeric (0-1).  Opacity of the fitted curves.
Default: \code{0.7}.}

\item{show_ic50_lines}{Logical.  Draw a vertical dashed line at each
compound's IC50.  Default: \code{FALSE}.}

\item{plot_title_size}{Numeric.  Font size for the plot title.  \code{NULL}
(default) uses \code{axis_title_size + 2}.}

\item{axis_line_color}{Character.  Colour of axis lines, ticks, and (when
\code{show_border = TRUE}) the panel border.  Default: \code{"black"}.}

\item{show_border}{Logical.  Draw a rectangular border around the plot
panel.  Default: \code{FALSE}.}

\item{transparent_background}{Logical.  If \code{TRUE}, the plot and panel
backgrounds are set to transparent.  Passed directly to
\code{\link[=plot_multiple_compounds]{plot_multiple_compounds()}}.  Default: \code{FALSE}.}

\item{verbose}{Logical.  Print progress messages.  Default: \code{TRUE}.}
}
\value{
Invisibly returns a named list with one entry per entity plotted.
Each entry contains:
\describe{
\item{\code{entity}}{The compound or construct name.}
\item{\code{plates}}{Character vector of plate names included.}
\item{\code{n_plates}}{Number of plates overlaid.}
\item{\code{plot}}{The \code{ggplot2} object.}
\item{\code{file}}{Full path to the saved PNG.}
}
}
\description{
\code{compare_plates_drc()} takes the output of \code{\link[=batch_drc_analysis]{batch_drc_analysis()}} and
generates one overlay plot per unique \strong{compound} or \strong{construct},
showing the fitted dose-response curves from every plate side-by-side.
This is the primary tool for assessing inter-plate reproducibility.

Each plot overlays the curves from all plates that contain the selected
construct-compound pair, with each plate rendered in a distinct colour.
Plots are always separated by the full \code{Construct:Compound} combination, so
the same compound tested on different constructs always produces separate
images.  The plot title is always \code{"Construct - Compound"} and the filename
is \code{Construct__Compound.png}.  Mean +- SD data points and error bars are
included by default.
}
\examples{
\dontrun{
# Run batch ratio + DRC analysis first
ratio_res <- batch_ratio_analysis("data/")
drc_res   <- batch_drc_analysis(ratio_res)

# Title shows compound name (default)
compare_plates_drc(drc_res, compare_by = "compound",
                   output_dir = "comparison_plots")

# Title shows construct name instead
compare_plates_drc(drc_res, compare_by = "construct",
                   output_dir = "comparison_plots")

# Only compare specific entities
compare_plates_drc(drc_res, compare_by = "compound",
                   selected_entities = c("Staurosporine", "Compound_A"))
}

}
\seealso{
\itemize{
\item \code{\link[=batch_drc_analysis]{batch_drc_analysis()}} - upstream function whose output is consumed here.
\item \code{\link[=plot_multiple_compounds]{plot_multiple_compounds()}} - underlying plotting engine.
}
}
